/** Shared layout strings. Tailwind v4 scans .ts sources, so these compile. */

/** The 2A canvas width with a safe-area-aware gutter (see `.gutter` in layout.css). */
export const container = 'mx-auto w-full max-w-[1240px] gutter';

/** Vertical rhythm for a full-width band. */
export const band = 'py-16 sm:py-20';
